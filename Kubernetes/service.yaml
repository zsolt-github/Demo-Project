apiVersion: v1
kind: Service
metadata:
  name: web-frontend
  namespace: development
  labels:
    app.kubernetes.i/name: web-frontend
spec:
  selector:
    type: LoadBalancer
    app.kubernetes.i/name: web-frontend
  ports:
    - port: 80
      name: http
      targetPort: 3000